cnt_queues = 8
dcb = False
data_mapping = True
mellanox_PCI = '07:00.1'
# intel_PCI = '81:00.1'

my_vhost0 = PMDPort(pci=mellanox_PCI,
  num_inc_q=cnt_queues,
  num_out_q=cnt_queues,
  data_mapping=data_mapping)
# my_vhost0 = PMDPort(name='test_interface', vdev='eth_vhost0,iface=/tmp/test.sock,queues='+str(cnt_queues))

# ================
bess.add_worker(0, 1)

for i in range(cnt_queues):
	QueueInc(port=my_vhost0, qid=i) -> \
	Sink()
